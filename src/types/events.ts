/**
 * Representa um evento no sistema
 * @property id - Identificador único do evento
 * @property name - Nome do evento
 * @property image - URL da foto do evento climático
 */
export type Event = {
    id: string;
    name: string;
    image: string;
  };